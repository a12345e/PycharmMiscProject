[project]
name = "pycharmmiscproject"
version = "0.1.0"
# pyspark 3.5 needs <= 3.11: its cloudpickle infinitely recurses on 3.14,
# and its Python worker crashes on 3.12.
requires-python = ">=3.10,<3.12"
dependencies = []
